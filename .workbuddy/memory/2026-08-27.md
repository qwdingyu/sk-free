# 2026-08-27

## 首页全局分析（PM + UI 视角）
- 分析对象：frontend/ 首页 SPA（index.html + src/main.js + src/broadcast/* + styles.css）。
- 关键发现（已逐字节验证）：`src/main.js`（1849 行）= `src/broadcast/*.js` 11 模块按顺序拼接产物，但 vite `npm run build` 不自动拼接 → 双份真相/手工同步，是文档 08 点名根因的重演（灾难风险 P0）。
- 产品定位对齐 docs/08：本产品是"时效性羊毛情报聚合站"，护城河 = 鲜度 + 可比性。
- 首页问题分层：① 工程架构炸弹（双份代码）；② 产品/IA（提交CTA错位、像数据库浏览器、鲜度低估）；③ 文案（四个名字：Sk-free/公益站导航/TOKEN站点速查/free.eforge.xyz）；④ 视觉收口（teal 双义等）。
- 最优方案分 5 步：Step0 拆炸弹（broadcast 改真 ESM、main.js 仅 import 入口）→ Step1 统一身份+价值主张 Hero → Step3 渲染纪律（持久铬只建一次、仅列表区重渲）→ Step2 今日可信速览前置 → Step4 视觉收口。
- 交付物：docs/17_首页全局分析与最优重构方案_2026-08-27.md + docs/17_首页IA对比.svg。
- 强项保留：死链可见沉底、鲜度圆点、档位配色、深浅主题/a11y/骨架屏、详情抽屉、结构化额度字段。

.DS_Store
broadcast/data/.local_backups/
worker/node_modules/
worker/.wrangler/
wrangler.toml.bak
.env

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example

# 前端测试用（scripts/test-frontend.mjs 需要 jsdom）
# 不带尾斜杠：node_modules 可能是符号链接（git 对 symlink 不按目录匹配，
# 只有 node_modules/ 会漏掉它，导致 status 一直显示 untracked）
node_modules

frontend/dist/
